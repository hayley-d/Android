var name : String = "Hayley"
println(name)

var age : Int = 21
println(age)

var female : Boolean = true
println(female)

//A constant is declared using val instead of ver
val myConstant : Double = 77.7
println(myConstant)

val variable = "Bun" //inferred that it is a String do not need to declare it